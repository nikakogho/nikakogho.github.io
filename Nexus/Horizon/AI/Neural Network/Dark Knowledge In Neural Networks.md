Knowledge a [[Artificial Neural Network|neural network]] keeps in non-argmax logits (parts of the output that are "wrong" or just not the actual final answer). Term coined by [[Geoffrey Hinton]].

Useful for [[Model Distillation]].